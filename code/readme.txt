## Steps to follow to run the quickstart app 

1. clone the app by running the command below : 
  $ git clone https://github.com/HyounesH/battlebrain2020.git
  
  
2. install required dependencies and start the project (backend & frontend)
  -- Backend
  $ cd battlebrain2020\code\seats_reservation 
  $ mvn clean install
  $ mvn spring-boot:run
  
  -- Frontend
  $ cd battlebrain2020\code\frontend 
  $ npm install
  $ ng serve
  
  
Info tu use to authenticate to the project :

=> Available User: 
username : yyounes
password: seat

Ps : there is data added after the backend got started
